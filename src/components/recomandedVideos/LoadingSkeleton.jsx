import React from "react";
import LoadingSkeleton from "../skeleton/Skeleton";
const RecmandedVideos = () => {
  // const [state, dispatch] = useContext(AppContext);
  return (
    <div>
      {/* <VideoCard /> */}
      <LoadingSkeleton />
    </div>
  );
};

export default RecmandedVideos;
